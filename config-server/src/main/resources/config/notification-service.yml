server:
   port: 9933
      
spring:
     datasource:
       url: jdbc:postgresql://database-1.cr4ucs664vca.eu-north-1.rds.amazonaws.com:5432/DemoDB
       username: postgres
       password: postgres
     jpa:
       hibernate:
         ddl-auto: update
       show-sql: true
     kafka:
       producer:
         bootstrap-servers: junaid360.servicebus.windows.net:9092
         key-serializer: org.apache.kafka.common.serialization.StringSerializer
         value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
       topic:
         name: send-email
       
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://host.docker.internal:8761/eureka/
  instance:
    hostname: host.docker.internal




